{
  "_port_note": "The step `arc_fixture_subject` has always named and never had. The probe schedules it, freezes it with the arc, thaws it and asserts the relative day survived — all of which works on a schedule ENTRY, so a card that does not exist was never noticed. It exists now, because a dangling reference in a fixture is still a dangling reference and the linter is about to say so.",
  "id": "fixture.subject_open",
  "version_scope": "fixture",
  "category": "team",
  "tick": "scheduled",
  "class": "interrupt",
  "tags": [
    "build_safe"
  ],
  "arc": "arc_fixture_subject",
  "scope": {
    "employee": {
      "type": "employee",
      "required": true
    }
  },
  "latch": {
    "one_shot": true
  },
  "options": [
    {
      "id": "ok",
      "outcome_id": "done",
      "effects": [
        {
          "verb": "set_flag",
          "name": "fixture_subject_landed"
        },
        {
          "verb": "end_arc",
          "arc_id": "arc_fixture_subject",
          "outcome": "closed"
        }
      ]
    }
  ],
  "text": {
    "tr": {
      "title": "Özne",
      "body": "{employee} hakkında.",
      "options": {
        "ok": "Tamam"
      }
    },
    "en": {
      "title": "Subject",
      "body": "About {employee}.",
      "options": {
        "ok": "OK"
      }
    }
  }
}
