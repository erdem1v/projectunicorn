# Y5.4 · After the first one (sealed)

```
id: Y5.4            arc: Y5 · Frank        node: 4/6        category: mentor        market: B2B (B2C has no churn edge; see notes)
trigger: event:customer_churned (first)      (today: EventBus.customer_churned from B2BSalesSystem._remove_lost, b2b_sales_system.gd:265-272, slot 4; the event queue runs slot 6, so the card lands the same day, after the loss is real)
prerequisites: market_type b2b   ·   [COND?] customers_lost_min 1  (GameState.run_customers_lost ≥ 1; no such condition today)   ·   interim JSON path: flag_set y2.4.outcome (Y2.4 Goodbye writes it)   ·   one_shot
slots: none  (Frank does not say the account's name; the loss is Y2.4's, this is the call after it)
one_shot: yes   ·   cooldown: n/a   ·   variants: 1   ·   weight: n/a
memory write: y5.4.choice → "heard"   · read by Y5.6 (verdict variant, if the rebuild adds variant picks), Y2.x (tone after the first loss)
speaker: char_mentor_frank   ·   surface: EventModal card, phone-origin; no clock (day-boundary beat)   ·   priority 6 (below the seed/gate beats, above ambients)
cross-reads: ← Y2.4 Goodbye (the loss)   ·   the "sayı olduğu gün" follow-up moves to Y7 as a diagnostic beat (no lead), per ruling
beat: 1 (single option, per ruling)
body: SEALED, byte-exact, nothing added around it. A/B/C below vary the title and subtitle line only.
title / subtitle:  A "Frank aradı / Telefon"   ·   B "Bir müşteri eksik / Frank aradı"   ·   C "Telefon / Frank"
                   A "Frank called / Phone"   ·   B "One account fewer / Frank called"   ·   C "Phone / Frank"
```

--- TR ---

*(sealed text, verbatim; identical under A, B and C)*

"Duydum. Gitti değil mi. İlk giden hep en çok acıtır, sonrakiler sadece sayı olur. Sayı olduğu gün ara beni; o gün başka bir sorun var demektir."

Seçenekler:
1. **Tamam** · effects: [ `set_flag{key: "y5.4.choice", value: "heard"}` ] · locked_unless: none

--- EN ---

*(sealed text, verbatim; identical under A, B and C)*

"I heard. They left. The first one always hurts most; the rest turn into numbers. Call me the day they turn into numbers. That day, the problem is something else."

Options:
1. **All right** · effects: [ `set_flag{key: "y5.4.choice", value: "heard"}` ] · locked_unless: none

--- INTENT ---

1. **Tamam / All right**: a beat with a memory. Nothing in the world moves; the reader is the Sales page (one account fewer) and the top bar (MRR down), both already true before the call. The flag is what later nodes read.

Notes for the director: body verbatim per ruling; single option per ruling; the earlier locked "Ara" door and the proposed Y5.4b are removed. Title/subtitle in three forms: A is the call, B is the count the game already knows, C is the channel only. B2C has no churn edge (audience erodes hourly, `churn_customer` on B2C trims audience silently), so a B2C Y5.4 needs `[COND?] audience_drop_edge`; not written. The account's name is deliberately absent from Frank's mouth.
