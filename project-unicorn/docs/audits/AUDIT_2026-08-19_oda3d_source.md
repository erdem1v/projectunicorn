# AUDIT 2026-08-19 — ODA 3D spike, Phase 0: where does the sealed render's geometry live?

**Finding: (a) — the sealed ODA render is a Three.js scene that lives in the repo.** No eyeballing needed, no PNG reverse-engineering.

- Scene construction: `tools/oda_render_rig/layers.html:54-145` (room shell, window wall + mullions, corkboard, three frames, desk, monitor/keyboard/lamp/mug/phone placement, incl. the director-authorized deltas: cork `(0.4155,1.2855,-0.955)` L95, lamp `(-1.04,T,-0.5)` yaw `1.48625` L122, phone `(0.38,T,-0.05)` yaw `-0.034907` L145). Prop meshes + material table `M`: `tools/oda_render_rig/desk-builders.js` (Box/Cylinder/Lathe/Extrude/Torus/Sphere geometry, `MeshStandardMaterial`).
- Light rigs: `layers.html:150-199` (day: hemi `0xfff1da/0x8a6f4d 0.85`, sun `0xffd6a0 3.4` from `(5.2,3.1,0.55)`→`(-1.4,0.3,-0.45)`, fill `0xfff6e8 0.4`; night: hemi `0x8a7154/0x241c12 0.3`, ceiling `PointLight(0xffd2a0,5,9,1.6)` at `(-0.1,2.55,0.4)`, lamp `SpotLight(0xffb866,9,1.2,0.45,0.5,1.7)` at the bulb aimed at `(-0.45,T,-0.45)`, `mouthGlow(0xffb866,0.45,0.32,1.8)`); mode switch (sky colour, exposure 1.05/1.0, dark-glass screens, bulb emissive 0.15/1.2) `layers.html:202-219`; tonemap ACES `layers.html:44-45`.
- Camera: pinned prose, not a tracked JSON — `[x,y,z,yaw°,pitch°,fov] = [0.298, 1.35, 1.56, 0.38, -1.91, 44]`, aspect 16:9, near 0.05 / far 40, look-at DERIVED (`camTarget`, `layers.html:624-627`) → target `(0.30463, 1.31667, 0.56058)`. Sources: `scripts/ui/oda/oda_layout.gd:50-52`, `tools/oda_render_rig/README.md:72`, `docs/design/oda_art_pipeline.md:74`. The only machine-readable copy is the gitignored `tools/oda_render_rig/out/anchors.json` (`cam`, projected `anchors`, alpha `regions`) in the main checkout.
- What is NOT in the repo: `office-room.html` (the Claude-Design page the rig copied from) — referenced only in comments; any `.glb/.gltf/.obj/.fbx` (none anywhere); a GLTFExporter run (`docs/design/oda_art_pipeline.md:22-29`: the design page had a "Download GLB" button, never used).
- Reference plates for the comparison gate: `tools/oda_render_rig/out/proof-{day,night}.png` (3840×2160, whole room incl. objects — the F5-judged proofs) and the tracked shipped plates `assets/art/center_view/room_{day,night}_3840x2160.png` (objects hidden).
- Layout contract: `scripts/ui/oda/oda_layout.gd` `RECTS` (hosts `board_outer/board_inner/frames_band/window/monitor/frame_outer_0..2`, props `lamp/mug/phone/keyboard`, `papers_zone`), `ART 3840×2160`. Tolerance is empirical (README.md:76-77): board hosts within ~6 px @3840, `frames_band`/`frame_outer_N` within 0.2 px, `monitor` within 0.6 px; smoke band gate ±0.001 normalized (`scripts/debug/endgame_smoke.gd:6079`).

**Consequence for the spike.** Route (a): export the assembled `room` group to GLB with Three.js's own `GLTFExporter` (r0.184, `binary:true`) from a page that is a verbatim copy of `layers.html:54-145` + `150-199` (`docs/tools/oda3d/export_glb.html`; only metadata delta = names on the 7 unnamed room materials so Godot can map them), import into Godot with `meshes/light_baking = Static Lightmaps` (UV2 generated at import). Every Godot placement then traces to a source line by construction; the camera is set from the same six numbers via the same derived target. Fallback (b) — GDScript primitives from the same lines — only if the Chrome/CDN export cannot run.
